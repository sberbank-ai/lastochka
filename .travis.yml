language: python

python:
  - "3.6"

before_install:
  - "pip install -U pip"

install:
  - pip install -r requirements.txt
  - python setup.py install
  - pip install codecov

script:
  - pytest
  - codecov